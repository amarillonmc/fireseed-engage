# 本地运行配置与安装锁 / Local runtime configuration and installation lock
config/local.php
config/installed.lock
config/.installing.lock
config/.install-token-consumed
config/.*-installer-backup-*.php
config/.fireseed-install-*

# 运行日志、备份与临时输出 / Runtime logs, backups, and temporary output
logs/
backups/
*.log
*.tmp

# 编辑器与操作系统文件 / Editor and operating-system files
.idea/
.vscode/
.DS_Store
Thumbs.db
